import { describe, it } from '@jest/globals';

describe('escrowIndexer edge cases', () => {
  it.todo('handles empty event batches without writing records');
  it.todo('skips malformed event payloads cleanly');
  it.todo('surfaces the unhappy path when the RPC layer fails');
});
